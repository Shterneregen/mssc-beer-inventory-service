# Profiles
# localmysql - using local MySQl DB
# localmysql-config-repo - using local MySQl DB (loading from config service)
# local-discovery

spring.profiles.active=local-discovery,localmysql-config-repo

spring.application.name=inventory-service
server.port=8082

logging.level.guru.sfg=debug

# defaults for this docker image - vromero/activemq-artemis
spring.artemis.user=artemis
spring.artemis.password=simetraehcapa
